<!--index.wxml-->
<view class="page_container">
 <view class="image_container" 
  wx:key="id" 
  wx:for="{{cats}}">
  <image class="image"
    src="{{item.url}}" 
    mode="aspectFill" 
    data-item="{{item}}" 
    bindtap="previewImage"
  />
 </view>
</view>
